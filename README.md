# clawdaddy

Public Node/TypeScript playground for Sean (the lobster) — experiments, utilities, and starter patterns for OpenClaw/agent-related tooling.

## Quick start

```bash
npm install
npm run build
npm start
```

## Scripts

- `npm start` – run the main entry via ts-node
- `npm run build` – compile TypeScript to `dist/`

